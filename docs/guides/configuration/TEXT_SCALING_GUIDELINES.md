# 📱 텍스트 스케일링 가이드라인 (Text Scaling Guidelines)

> **디바이스 글자 크기 설정 처리 가이드**
> **⚠️ 접근성과 레이아웃 안정성의 균형을 맞추는 것이 중요합니다.**

---

## 📋 개요

사용자가 디바이스의 글자 크기를 조정하면, 앱의 텍스트도 함께 확대/축소됩니다. 이는 접근성을 향상시키지만, 레이아웃이 깨지거나 중요한 정보가 잘릴 수 있습니다.

Milkyway 앱은 **하이브리드 접근법**을 사용하여 접근성과 레이아웃 안정성을 모두 확보합니다.

---

## 🎯 현재 설정

### 전역 텍스트 스케일 제한
`lib/main.dart`에서 앱 전체에 대해 텍스트 스케일을 제한하고 있습니다:

```dart
builder: (context, child) {
  return MediaQuery(
    data: MediaQuery.of(context).copyWith(
      textScaler: MediaQuery.of(context).textScaler.clamp(
            minScaleFactor: 0.8,  // 최소 80%
            maxScaleFactor: 1.2,  // 최대 120%
          ),
    ),
    child: child ?? const SizedBox.shrink(),
  );
},
```

**효과**: 사용자가 디바이스 글자 크기를 매우 크게 설정해도, 앱 내 텍스트는 최대 120%까지만 확대됩니다.

---

## 📐 하이브리드 접근법

### 1. **중요한 UI 요소 (고정 필요)**

다음 요소들은 레이아웃이 깨지지 않도록 **텍스트 스케일을 1.0으로 고정**합니다:

#### ✅ 고정해야 할 요소
- **제목 (Titles)**: 화면 제목, 섹션 제목
  - 예: "책 상세페이지", "책 소개", "책 메모"
- **카드 제목**: 책 제목, 메모 제목 등
  - 예: 책 상세 화면의 책 제목
- **버튼 텍스트**: 주요 액션 버튼의 텍스트
  - 예: "메모하기", "다음", "저장", "삭제"
- **네비게이션 바**: 하단 네비게이션 바의 라벨
  - 예: "Home", "Books", "Memos", "Profile"
- **필터 버튼**: 상태 필터 버튼
  - 예: "읽고 싶은", "읽는 중", "완독"
- **AppBar 타이틀**: 상단 앱바의 제목

#### 구현 방법
```dart
MediaQuery(
  data: MediaQuery.of(context).copyWith(
    textScaleFactor: 1.0, // 또는 textScaler: TextScaler.linear(1.0)
  ),
  child: Text(
    '책 제목',
    style: const TextStyle(fontSize: 20),
    maxLines: 3,
    overflow: TextOverflow.ellipsis,
  ),
)
```

---

### 2. **본문 텍스트 (사용자 설정 따름)**

다음 요소들은 **사용자의 글자 크기 설정을 따릅니다** (0.8~1.2 범위 내):

#### ✅ 사용자 설정을 따를 요소
- **책 소개**: 책 설명 텍스트
- **메모 내용**: 메모 본문
- **일반 설명 텍스트**: 안내 문구, 부가 설명
- **입력 필드 힌트**: TextField의 hintText
- **에러 메시지**: 유효성 검사 에러 메시지
- **스낵바 메시지**: 성공/에러 스낵바

#### 구현 방법
```dart
// MediaQuery로 감싸지 않으면 자동으로 사용자 설정을 따름
Text(
  '책 소개 내용...',
  style: const TextStyle(fontSize: 12),
)
```

---

## 🔍 적용 예시

### 책 상세 화면

```dart
// ✅ 책 제목: 고정 (레이아웃 보호)
MediaQuery(
  data: MediaQuery.of(context).copyWith(textScaleFactor: 1.0),
  child: SizedBox(
    height: 84,
    child: Text(
      book.title,
      maxLines: 3,
      overflow: TextOverflow.ellipsis,
    ),
  ),
),

// ✅ 저자, 출판사: 고정 (레이아웃 보호)
MediaQuery(
  data: MediaQuery.of(context).copyWith(textScaleFactor: 1.0),
  child: Text(book.author),
),

// ✅ 책 소개: 사용자 설정 따름 (접근성)
Text(
  book.description,
  style: const TextStyle(fontSize: 12),
),
```

### 버튼

```dart
// ✅ 버튼 텍스트: 고정 (레이아웃 보호)
MediaQuery(
  data: MediaQuery.of(context).copyWith(textScaleFactor: 1.0),
  child: ElevatedButton(
    onPressed: () {},
    child: const Text('메모하기'),
  ),
),
```

### 네비게이션 바

```dart
// ✅ 네비게이션 바 라벨: 고정 (레이아웃 보호)
MediaQuery(
  data: MediaQuery.of(context).copyWith(textScaleFactor: 1.0),
  child: Text(
    label,
    style: const TextStyle(fontSize: 10),
    maxLines: 1,
    overflow: TextOverflow.ellipsis,
  ),
),
```

---

## 🎨 디자인 원칙

### 1. 접근성 우선
- **본문 텍스트는 사용자 설정을 따름**: 가독성이 중요한 콘텐츠는 사용자가 원하는 크기로 조정 가능
- **전역 제한 (0.8~1.2)**: 극단적인 크기 변화로 인한 레이아웃 붕괴 방지

### 2. 레이아웃 안정성
- **중요한 UI 요소는 고정**: 제목, 버튼, 네비게이션 등은 항상 일정한 크기 유지
- **고정 높이 요소**: `SizedBox`로 높이가 고정된 요소는 반드시 텍스트 스케일 고정

### 3. 일관성
- **동일한 유형의 요소는 동일하게 처리**: 모든 제목은 고정, 모든 본문은 사용자 설정 따름

---

## 🛠️ 유틸리티 함수

반복되는 패턴을 줄이기 위해 유틸리티 함수를 사용할 수 있습니다:

```dart
// lib/core/utils/text_scaling_helper.dart
class TextScalingHelper {
  /// 텍스트 스케일을 1.0으로 고정하는 MediaQuery 래퍼
  static Widget withFixedTextScale({
    required BuildContext context,
    required Widget child,
  }) {
    return MediaQuery(
      data: MediaQuery.of(context).copyWith(
        textScaleFactor: 1.0,
      ),
      child: child,
    );
  }
}

// 사용 예시
TextScalingHelper.withFixedTextScale(
  context: context,
  child: Text('고정된 텍스트'),
)
```

---

## 📊 적용 우선순위

### 높은 우선순위 (즉시 적용)
1. ✅ **책 제목** (책 상세 화면) - 이미 적용됨
2. ⚠️ **AppBar 타이틀** - 모든 화면의 AppBar 제목
3. ⚠️ **버튼 텍스트** - 주요 액션 버튼
4. ⚠️ **네비게이션 바 라벨** - 하단 네비게이션 바

### 중간 우선순위
5. **필터 버튼** - 상태 필터, 메모 필터
6. **카드 제목** - 메모 카드, 책 카드의 제목
7. **섹션 제목** - "책 소개", "책 메모" 등

### 낮은 우선순위 (사용자 설정 따름)
- **본문 텍스트** - 책 소개, 메모 내용
- **설명 텍스트** - 안내 문구, 부가 설명
- **입력 필드 힌트** - TextField의 hintText

---

## ⚠️ 주의사항

### 1. 과도한 고정 금지
- 모든 텍스트를 고정하면 접근성이 저해됨
- 본문 텍스트는 반드시 사용자 설정을 따름

### 2. 테스트 필수
- 다양한 글자 크기 설정에서 테스트
- iOS: 설정 > 화면 표시 및 밝기 > 텍스트 크기
- Android: 설정 > 접근성 > 글자 크기

### 3. 레이아웃 유연성
- 고정 높이(`SizedBox`)를 사용하는 경우 반드시 텍스트 스케일 고정
- `Expanded`나 `Flexible`을 사용하면 더 유연하게 대응 가능

---

## 📚 참고 자료

- [Flutter Accessibility - Text Scaling](https://docs.flutter.dev/accessibility-and-localization/accessibility)
- [Material Design - Typography](https://m3.material.io/styles/typography/overview)
- [iOS Human Interface Guidelines - Dynamic Type](https://developer.apple.com/design/human-interface-guidelines/typography)
- [Android Accessibility - Font Size](https://developer.android.com/guide/topics/ui/accessibility/font-size)

---

## 🔄 변경 이력

### 2025-11-20
- **책 제목 텍스트 스케일 고정** - 책 상세 화면의 책 제목에 적용
- **가이드라인 문서 작성** - 하이브리드 접근법 정리

---

**마지막 업데이트**: 2025-11-20  
**작성자**: AI Assistant  
**검토자**: 개발팀  
**다음 검토 예정일**: 2025-12-20

